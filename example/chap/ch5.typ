#let ch5 = [
  = 系统实现

  == 模块划分

  本章展示一个 dummy 系统的实现结构，包含前端、后端与数据层。

  == 接口示例

  - GET /api/health: 返回服务状态。
  - POST /api/evaluate: 接收输入并返回评分。
  - GET /api/report: 下载实验报告。

  == 工程说明

  实现部分仅用于展示章节结构，不绑定具体语言与框架，便于迁移到真实项目。
]
