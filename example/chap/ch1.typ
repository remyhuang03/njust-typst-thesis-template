#let ch1 = [
  = 引言

  == 写作目的

  本章节使用 dummy 文本演示论文正文样式。示例内容覆盖段落、枚举、引用、图表和多级标题，
  便于在正式写作前验证模板行为与编译结果@dummy2024。

  #lorem(300)

  == 结构预览

  === 多级标题示例

  这是三级标题下的一段说明文字，用于确认目录、标题编号与样式配置是否生效。

  === 列表示例

  - 第一项：用于展示正文中的无序列表。
  - 第二项：用于模拟需求清单。
  - 第三项：用于观察段间距和缩进。

  == 示例图片

  #figure(
    image("fig/sticker.webp", width: 45%),
    caption: [Dummy 示例图片：模板内置素材],
  ) <fig:dummy_image>

  如@fig:dummy_image 所示，图片可以正常编号、交叉引用并进入图目录。

  == 示例表格

  #figure(
    table(
      columns: (auto, auto, auto),
      table.header([编号], [要素], [说明]),
      [1], [正文], [章节主体内容与段落排版],
      [2], [多级标题], [二级到四级标题编号展示],
      [3], [图表], [图片和表格的插入、编号与引用],
    ),
    caption: [Dummy 示例表格：功能覆盖清单],
  ) <tab:feature_list>

  表@tab:feature_list 用于展示典型论文功能点。
]
