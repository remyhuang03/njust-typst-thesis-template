#let ch2 = [
  = 背景与基础

  == 模板中常见元素

  本章用简短文本说明论文模板中的基础组件，包括引用、数学公式、段落与章节结构。

  == 公式示例

  在此给出一个用于排版测试的公式：

  $
  S = alpha x + beta y + gamma z
  $<eq:1>

  当 $alpha + beta + gamma = 1$ 时，上#ref(<eq:1>)可用于构造简单加权指标。

  == 引用示例

  这里使用第二条参考文献进行测试引用@dummy2025，观察参考文献列表与文内编号是否正确。
]
